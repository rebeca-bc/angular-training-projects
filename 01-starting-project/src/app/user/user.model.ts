// type User = {
//   id: string;
//   name: string;
//   avatar: string;
// };

// interfaces are only for objects, type can be other things 
export interface User {
    id: string;
    name: string;
    avatar: string;
};